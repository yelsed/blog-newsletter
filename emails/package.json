{
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "maizzle serve",
    "build": "maizzle build production",
    "deploy": "maizzle build production && node scripts/sync-images.js && node scripts/copy-to-laravel.js"
  },
  "dependencies": {
    "@maizzle/framework": "^5.5.0",
    "tailwindcss-preset-email": "^1.4.1"
  }
}
